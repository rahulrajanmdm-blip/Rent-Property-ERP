rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Prototype rule: Allow read and write for Dream Dwell ERP instances (dev and published)
      allow read, write: if true;
    }
  }
}
